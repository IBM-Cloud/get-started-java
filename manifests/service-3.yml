apiVersion: v1
kind: Service
metadata:
    name: "myaksdemo-f366"
    labels:
        app: "myaksdemo-f366"
spec:
    type: LoadBalancer
    ports:
    - port: 9080
      targetPort: 9080
      protocol: TCP
      name: http
    selector:
        app: "myaksdemo-f366"